div
	if withTitle
		div(class="headline dark size-20")="Tracks"
		div(class="headline light size-14")="View all"
	ul(class=tracksClassName)
		if tracks.length > 0
			each track in tracks
				li(class="tracks-list__item" id=`track-item-${track.id}`)