import BaseComponent from '../../BaseComponent/BaseComponent';
import template from './Following.pug';

class Following extends BaseComponent {
	constructor() {
		super(template);
	}
}

export default Following;